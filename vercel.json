{
  "version": 2,
  "builds": [
    {
      "src": "api/clone-repo/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["api/**/*.ts"],
        "zeroConfig": false
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}
